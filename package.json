{
  "name": "repl_runtime",
  "private": true,
  "version": "0.3.1",
  "engines": {
    "node": ">=20.19"
  },
  "description": "In-browser REPLs for beginners: Python, Lua and JavaScript, one route per language.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "assets": "node scripts/copy-pyodide.mjs",
    "dev": "npm run assets && vite",
    "build": "npm run assets && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "acorn": "^8.18.0",
    "pyodide": "^314.0.7",
    "xterm-readline": "^1.2.2"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
